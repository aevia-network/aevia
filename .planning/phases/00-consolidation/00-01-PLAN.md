---
phase: 00-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git (merge commit)
  - apps/video/src/lib/p2p/chunk-relay.ts
  - apps/video/src/lib/p2p/pubsub-tracker.ts
  - apps/video/src/lib/webrtc/whep.ts
  - apps/video/src/lib/mesh/resolve.ts
  - apps/video/e2e/p2p-hls-multiviewer.spec.ts
autonomous: false
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "Branch feat/frontend-phase2 is merged into main with zero conflicts"
    - "Cloudflare Pages production (aevia.video) serves the post-merge bundle"
    - "Production bundle contains the /hls/index.m3u8 path and no /playlist.m3u8 reference"
    - "Production bundle contains the hls.js failover rotation logic (e6a3ff3) and the pubsub-tracker stub (7810ccc) and the F4 Playwright spec (b444ed7)"
  artifacts:
    - path: "apps/video/e2e/p2p-hls-multiviewer.spec.ts"
      provides: "F4 multi-viewer smoke spec on main"
      contains: "AEVIA_E2E_LIVE_URL"
    - path: "apps/video/src/lib/p2p/pubsub-tracker.ts"
      provides: "pubsub tracker stub file exists on main"
  key_links:
    - from: "main branch HEAD"
      to: "aevia.video production deployment"
      via: "Cloudflare Pages git integration"
      pattern: "pages production bound to main"
---

<objective>
Land `feat/frontend-phase2` on `main` and promote it to the `aevia.video` Cloudflare Pages production deployment so production and the preview URL `f049801b.aevia-video.pages.dev` converge.

Purpose: Close DEPLOY-01. Three frontend commits have been shipped on a worktree branch but are invisible in production — `e6a3ff3` (hls.js failover rotation), `7810ccc` (pubsub-tracker stub + `?tracker=pubsub` no-op flag), `b444ed7` (F4 multi-viewer Playwright smoke). Until they land on `main`, Phase 1 (M9 audio) cannot start from a clean production baseline.

Output: merge commit on `main`, production `aevia.video` bundle updated, preview URL retired as informational-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-consolidation/00-CONTEXT.md
@.planning/phases/00-consolidation/00-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run merge feat/frontend-phase2 → main and resolve conflicts if any</name>
  <files>
    - Working tree: /Users/leandrobarbosa/Personal/videoengine (main worktree)
    - Feature branch: feat/frontend-phase2 (currently checked out in /Users/leandrobarbosa/Personal/aevia-frontend-phase2)
  </files>
  <read_first>
    - CLAUDE.md (attribution rules — NO Co-Authored-By, NO AI-tool attribution in merge commit)
    - commitlint.config.mjs (valid scopes — merge commit scope is `video`)
    - .planning/phases/00-consolidation/00-CONTEXT.md §Specific Ideas ("Preview-to-prod promotion for frontend is a simple `merge + redeploy` — CF Pages has no staging-promote step")
  </read_first>
  <action>
    Execute on /Users/leandrobarbosa/Personal/videoengine (the main worktree):

    1. `git -C /Users/leandrobarbosa/Personal/videoengine fetch --all --prune` — pull the three tip commits (`e6a3ff3`, `7810ccc`, `b444ed7`) of feat/frontend-phase2.
    2. `git -C /Users/leandrobarbosa/Personal/videoengine checkout main` and `git pull --ff-only origin main` to rule out divergence.
    3. Dry-run the merge to detect conflicts without modifying the tree: `git -C /Users/leandrobarbosa/Personal/videoengine merge --no-commit --no-ff feat/frontend-phase2`. If git reports zero conflicts, `git merge --abort` immediately (we will re-do the merge for real in task 2 with the proper commit message).
    4. If conflicts ARE reported: enumerate the conflicting paths, inspect each with `git status` + `git diff`, and resolve them. Expected conflict surface is zero because `feat/frontend-phase2` was branched from `d424fa4` (the common ancestor of both phase-2 worktrees) and both worktrees are known to have non-overlapping file sets (frontend: `apps/video/**`; backend: `services/provider-node/**`). If any overlap exists it is almost certainly a stale lockfile or snapshot image — resolve by taking the frontend-branch version for `apps/video/**` and the main version for everything else, then run `pnpm install --frozen-lockfile` if `pnpm-lock.yaml` was touched.
    5. After any resolution, verify locally that `pnpm -w run typecheck` and `pnpm -w exec biome check` pass across `apps/video/` (Phase 0 Strong gate check #1 for the frontend side).
    6. Leave the tree on `main` with the merge staged OR aborted — task 2 takes it from here with the real merge commit.
  </action>
  <verify>
    <automated>cd /Users/leandrobarbosa/Personal/videoengine && git log --oneline feat/frontend-phase2 ^main | tr '\n' ' ' | grep -q 'b444ed7' &amp;&amp; git log --oneline feat/frontend-phase2 ^main | grep -q '7810ccc' &amp;&amp; git log --oneline feat/frontend-phase2 ^main | grep -q 'e6a3ff3' &amp;&amp; echo OK_THREE_COMMITS_AHEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/leandrobarbosa/Personal/videoengine log --oneline feat/frontend-phase2 ^main` lists exactly three commits: `b444ed7`, `7810ccc`, `e6a3ff3`
    - `git -C /Users/leandrobarbosa/Personal/videoengine status` reports "nothing to commit, working tree clean" on branch `main` OR reports a staged merge with zero unresolved conflicts
    - `pnpm -w run typecheck` exit code 0 when run with the feat/frontend-phase2 tip staged
    - `pnpm -w exec biome check apps/video` exit code 0 when run with the feat/frontend-phase2 tip staged
    - No conflicts in `apps/video/src/lib/p2p/`, `apps/video/src/lib/webrtc/`, or `apps/video/e2e/`
  </acceptance_criteria>
  <done>Merge is either cleanly fast-forwardable or any conflicts are resolved in the staged tree; typecheck + biome green; working tree on `main`.</done>
</task>

<task type="auto">
  <name>Task 2: Merge feat/frontend-phase2 into main with correct commit message and push</name>
  <files>
    - /Users/leandrobarbosa/Personal/videoengine (main worktree)
    - .git (merge commit on main)
  </files>
  <read_first>
    - CLAUDE.md §Authorship and attribution (the commit author is `git config user.name` / `user.email` — NO Co-Authored-By, NO vendor names, NO AI-tool attribution, NO 🤖 signatures)
    - commitlint.config.mjs (valid scope: `video`; NOT `consolidation`, NOT `phase`, NOT `claude`)
    - .planning/phases/00-consolidation/00-PATTERNS.md §Commit scope
  </read_first>
  <action>
    Execute on /Users/leandrobarbosa/Personal/videoengine with `main` checked out:

    1. If task 1 aborted the dry-run merge, start the real merge now: `git merge --no-ff feat/frontend-phase2 -m "$(cat <<'EOF'
feat(video): hls.js failover rotation + pubsub tracker stub + F4 smoke

Bring feat/frontend-phase2 into main:
  e6a3ff3 feat(video): rotate hls.js loadSource across mesh failover candidates
  7810ccc feat(video): pubsub tracker stub + ?tracker=pubsub no-op flag
  b444ed7 test(video): operator-run p2p+hls multi-viewer smoke

Closes DEPLOY-01 (Phase 0). Unblocks production gate checks 4 + 6 on
aevia.video. Phase 1 (M9 audio) can now start from a clean baseline.
EOF
)"` — use the HEREDOC form exactly; the subject line uses the `video` commitlint scope. Do NOT add Co-Authored-By trailers. Do NOT add AI-tool attribution. The git author identity (`git config user.name` / `user.email`) is the sole authorship signal.
    2. If task 1 already staged a merge with conflict resolutions, commit it now with the same message above via `git commit --no-edit -m "..."` (edit-in-place with the same HEREDOC).
    3. Run `git push origin main` and capture the push output (SHA of the new merge commit on origin).
    4. On the sibling worktree path `/Users/leandrobarbosa/Personal/aevia-frontend-phase2`, do NOT delete yet — that happens in Plan 00-06 after the Strong gate passes. Worktree cleanup is centralised there per the worktree-per-task discipline in CLAUDE.md `feedback_worktree_per_task`.
  </action>
  <verify>
    <automated>cd /Users/leandrobarbosa/Personal/videoengine &amp;&amp; git log --oneline main -1 | grep -q 'feat(video): hls.js failover rotation' &amp;&amp; git log --format='%B' -1 main | grep -vq 'Co-Authored-By' &amp;&amp; git log --format='%B' -1 main | grep -viq 'claude\|anthropic\|generated with' &amp;&amp; echo OK_MERGE_CLEAN_ATTRIBUTION</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/leandrobarbosa/Personal/videoengine log --oneline main -1` shows a merge commit with subject matching regex `^feat\(video\): hls\.js failover rotation`
    - `git -C /Users/leandrobarbosa/Personal/videoengine log --format='%B' -1 main` body contains NONE of: `Co-Authored-By`, `Claude`, `Anthropic`, `generated with`, `🤖`, `Cursor`, `Copilot`
    - `git -C /Users/leandrobarbosa/Personal/videoengine log --format='%an %ae' -1 main` prints `Leandro Barbosa leandrobar93@gmail.com` (the configured git identity) and nothing else
    - `git ls-tree -r main --name-only | grep -E 'apps/video/e2e/p2p-hls-multiviewer.spec.ts|apps/video/src/lib/p2p/pubsub-tracker.ts'` returns both paths
    - `git rev-parse origin/main` matches `git rev-parse main` (push succeeded)
  </acceptance_criteria>
  <done>main contains the three frontend-phase2 commits; merge commit author is Leandro only; `origin/main` is updated; worktree `aevia-frontend-phase2` left intact (cleanup in 00-06).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Cloudflare Pages production deploy picks up main and serves the new bundle</name>
  <files>
    - https://aevia.video/ (production)
    - Cloudflare Pages dashboard for project `aevia-video`
  </files>
  <read_first>
    - .planning/phases/00-consolidation/00-CONTEXT.md §Validation Gate (gate check #4 — bundle grep `/hls/index.m3u8` ≥ 1, `/playlist.m3u8` = 0)
    - CLAUDE.md §Stack invariants ("Cloudflare only (Pages + Workers + R2 + Stream + KV). No Vercel.")
    - apps/video/next.config.ts OR wrangler.toml OR `.cloudflare/` to confirm production project name
  </read_first>
  <what-built>
    Task 2 pushed `main` to origin. CF Pages has a git integration bound to the `aevia-video` project's production branch = `main`. The push automatically triggers a production build. No manual `wrangler pages deploy` is required — CF Pages handles it.
  </what-built>
  <how-to-verify>
    1. Wait ~90-180s for the CF Pages build to finish. Tail build progress: open https://dash.cloudflare.com/?to=/:account/pages/view/aevia-video (account is the aevia CF account). The latest build should reference the new merge commit SHA from task 2.
    2. When status shows "Success" on the production deployment:
       a. Fetch the production bundle JS map: `curl -s https://aevia.video/ | grep -oE 'static/chunks/[^"]+\.js' | head -5` to list the top chunk filenames.
       b. For each chunk filename, verify `/hls/index.m3u8` appears at least once: `for f in $(curl -s https://aevia.video/ | grep -oE '/_next/static/chunks/[^"]+\.js' | sort -u); do curl -s "https://aevia.video${f}" | grep -c '/hls/index.m3u8'; done` — sum should be ≥ 1.
       c. For each chunk filename, verify `/playlist.m3u8` appears EXACTLY zero times: replace `grep -c '/hls/index.m3u8'` with `grep -c '/playlist.m3u8'` in the above loop — sum must be 0.
       d. Open https://aevia.video/ in a clean browser window. Console MUST NOT show any errors. The hero/creator-default.jpg must load. The page must render (no blank-screen regression).
    3. Confirm the preview URL is now a redundant mirror: `curl -sI https://f049801b.aevia-video.pages.dev/ | head -1` should still return 200 but the deployed SHA (visible in the CF dashboard for that preview) matches the main-branch deploy — preview and prod have converged.

    If CF Pages build FAILS (red status on the dashboard):
      - Capture the full build log output to the reply.
      - Check for pnpm lockfile drift (`pnpm-lock.yaml` updated post-merge but not committed) — fix by running `pnpm install` locally and committing the lockfile update with scope `deps`.
      - Check for Biome lint failures that were green locally but fail in CF's Node.js version — diagnose via `pnpm -w exec biome check apps/video` and inspect the CF Node version in the build log.
      - Do NOT disable `AEVIA_DEV_BYPASS_AUTH` or any prod flag as a workaround; see CLAUDE.md memory `feedback_env_local_leak_prod`.

    Reply with:
      - Production deployment SHA (short, matches `git rev-parse --short main`)
      - Bundle grep counts: `/hls/index.m3u8` = N, `/playlist.m3u8` = M (M MUST be 0)
      - Page loads cleanly: yes / no + console errors if any
      - One of: "approved" (all green) OR "blocked: &lt;reason&gt;" (detailed)
  </how-to-verify>
  <resume-signal>Type "approved" when bundle grep passes and page loads. If blocked, describe the CF Pages build error or bundle grep mismatch so the plan can iterate — do NOT revert the merge (we are fix-forward per D-04).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer laptop → origin (GitHub) | Merge commit pushed; any injected content becomes authoritative for CF Pages build |
| origin (GitHub) → Cloudflare Pages | CF pulls main on push event; inherits whatever landed in the merge |
| Cloudflare Pages → browser (aevia.video) | Public bundle served to every visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-01 | Tampering | merge commit on main | mitigate | Commit attribution assertion in acceptance criteria blocks merges authored by anyone other than Leandro; grep for `Co-Authored-By` / `Claude` / `Anthropic` in commit body rejects injected attribution |
| T-00-01-02 | Information Disclosure | production bundle | mitigate | Task 3 verifies `/playlist.m3u8` count = 0 (prevents accidental shipping of the old legacy manifest path); bundle grep catches a bad build before operator approves |
| T-00-01-03 | Denial of Service | CF Pages production | accept | Fix-forward per D-04 — if the deploy breaks, operator triggers revert-and-redeploy from the CF dashboard, no automation in this plan |
| T-00-01-04 | Elevation of Privilege | pnpm-lock.yaml drift | mitigate | Task 1 runs `pnpm install --frozen-lockfile` if lockfile touched; any update must land in its own commit with `deps` scope, preventing silent transitive dependency swap inside a `video` scope merge |
</threat_model>

<verification>
1. Git-history assertion (task 2 automated): merge commit on main; three feature commits present; author is Leandro only; no Co-Authored-By.
2. Pre-push typecheck + biome (task 1 automated): zero errors in `apps/video/`.
3. CF Pages production build (task 3 human-verify): status = Success; deployment SHA = `main` HEAD.
4. Production bundle grep (task 3 human-verify): `/hls/index.m3u8` ≥ 1 AND `/playlist.m3u8` = 0 across all chunks.
5. Production smoke (task 3 human-verify): https://aevia.video/ renders, zero console errors.
</verification>

<success_criteria>
- `main` on origin contains the three frontend-phase2 commits (`e6a3ff3`, `7810ccc`, `b444ed7`) via a merge commit with scope `video` and Leandro-only attribution.
- `aevia.video` production serves a bundle that contains `/hls/index.m3u8` (≥1 hit) and no `/playlist.m3u8` (0 hits).
- Preview URL `f049801b.aevia-video.pages.dev` and production `aevia.video` point at the same commit.
- `apps/video/e2e/p2p-hls-multiviewer.spec.ts` exists on `main` (later used by the Strong gate check #6 in Plan 00-06).
- Worktree `aevia-frontend-phase2` left intact (cleanup deferred to Plan 00-06).
</success_criteria>

<output>
After completion, create `.planning/phases/00-consolidation/00-01-SUMMARY.md` with:
- Merge commit SHA on `main`
- CF Pages production deployment ID + URL
- Bundle grep counts (confirmation of gate check #4 partial — full gate execution in Plan 00-06)
- Worktree status note: `aevia-frontend-phase2` retained for cleanup in Plan 00-06
</output>
